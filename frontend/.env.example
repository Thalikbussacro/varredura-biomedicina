# URL da API (opcional, padrão: http://localhost:3001/api)
VITE_API_URL=http://localhost:3001/api

# Forçar uso de dados estáticos (JSON) ao invés da API
# true = sempre usar JSON
# false (ou não definido) = tentar API primeiro, fallback para JSON se falhar
VITE_USE_STATIC_DATA=false
